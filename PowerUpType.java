package de.tum.cit.ase.bomberquest.map;
/**
 * Enum representing the different types of Power-Ups available in the game.
 * Each Power-Up provides a unique effect to enhance the player's abilities.
 */
public enum PowerUpType {
    /** Increases the bomb's explosion radius, allowing it to affect a larger area. */
    BLAST_RADIUS,
    /** Increases the number of bombs the player can place at the same time. */
    CONCURRENT_BOMBS,
    //TIME_EXTENSION
}
